---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: microsoft/dotnet

params:
  MAVEN_OPTS:
  MAVEN_CONFIG:
  SONAR_HOST_URL:

inputs:
  - name: game-monitor-repo

run:
  path: game-monitor-repo/ci/build-dotnet.sh